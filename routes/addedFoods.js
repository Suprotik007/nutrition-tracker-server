const express = require('express');
const router = express.Router();

// Define your routes
router.get('/', (req, res) => {
  // Return all foods
});

router.post('/', (req, res) => {
  // Add a new food item
});

module.exports = router;
